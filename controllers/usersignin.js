const {getAll} = require('../models/usermodels');
const bcrypt = require('bcrypt');
const jwt = require('jsonwebtoken');

const functName = (req,rec) => {

}

const funcNameTwo = (req,res) => {

}

module.exports = {
    functName,
    funcNameTwo
}